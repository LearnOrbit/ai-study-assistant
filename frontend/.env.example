# Leave /api for local development; Vite forwards it to the backend.
# For a separately hosted backend, use its public HTTPS URL ending in /api.
VITE_API_BASE_URL=/api

# Optional development proxy override (set in the shell before npm run dev).
# API_PROXY_TARGET=http://127.0.0.1:8000
